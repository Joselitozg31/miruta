export default function Logo() {
  return (
    <img
      src="/logo.png" // Asegúrate de que esta ruta sea correcta
      alt="App Logo"
      className="logo" // Aplica la clase global
    />
  );
}
